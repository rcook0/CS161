# Proof Hooks

Hooks generate evidence bundles per theorem:
- `events.jsonl` (trace)
- `summary.json` (counts + metadata)

v1.3 adds golden verification: `golden_verify.py` ensures hooks remain stable after refactors.
